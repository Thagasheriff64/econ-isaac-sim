# Backups created by the installer inside the Isaac Sim folder (not here, but just in case)
*.kit.bak

# Python / OS cruft
__pycache__/
*.pyc
.DS_Store

# Local raw screenshots (curated copies live in docs/images/)
Pictures/

# Raw screen recordings (commit the compressed mp4 in videos/ instead)
docs/**/*.webm
